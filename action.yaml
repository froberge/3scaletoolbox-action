name: install-3scale-toolbox
description: This is an action that install 3scale toolbox to update and API
author: Felix Roberge
inputs:
  api-to-upload:
    description: 'The API you need to upload'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.api-to-upload }}